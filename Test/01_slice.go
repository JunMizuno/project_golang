package main

import (
	"fmt"
)

var (
	msg1 = "Hello"

	example = []string {
		"Golang",
		"hands-on",
		"in",
		"Osaka",
	}
)

var msg2 string

func main() {
	msg2 = "end"
	// Printlnは「改行あり」の関数
	//fmt.Println(msg1, example, msg2);

	// Printは「改行なし」
	/*
	for i := 0; i < 4; i++ {
		fmt.Print(i, i)
	}
	fmt.Print("\n")
	*/

	
}